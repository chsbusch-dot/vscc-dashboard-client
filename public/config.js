// Placeholder runtime config. In the Docker image, the entrypoint overwrites
// this with `window.VSCC_HOST = "<backend-ip>"` when the VSCC_HOST env var is
// set; otherwise the app targets the host it is served from.
